---
- name: Prepare
  hosts: all
  gather_facts: false
  tasks:
    - name: Install requirements
      ansible.builtin.apt:
        name:
          - sudo
          - gnupg2
          - ca-certificates
          - curl
        state: present
        update_cache: true
